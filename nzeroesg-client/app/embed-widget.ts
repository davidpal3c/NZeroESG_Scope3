// 'use client';
// import ReactDOM from "react-dom/client";
// import ChatInterface from "@/app/components/chat_ui/ChatInterface";

// export function init(targetId: string, options: { apiUrl: string; apiKey: string }) {
//   const container = document.getElementById(targetId);
//   if (!container) return;

//   const root = ReactDOM.createRoot(container);
//   root.render(<ChatInterface />);
// }


// apiUrl={options.apiUrl} apiKey={options.apiKey} 